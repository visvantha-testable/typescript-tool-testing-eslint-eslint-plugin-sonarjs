# opentelemetry-examples (reference)

Vendored from [sakthisudarshan/opentelemetry2](https://github.com/sakthisudarshan/opentelemetry2).

This subdirectory is **not** used for Path Coverage % validation. Use the repo root trigger:

```bash
npm run trigger
npm run verify
```

See root [METRIC_COVERAGE.md](../METRIC_COVERAGE.md).
