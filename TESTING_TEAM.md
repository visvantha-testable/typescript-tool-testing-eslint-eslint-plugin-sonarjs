# Testing Team — Path Coverage % (eslint + sonarjs)

```bash
npm install && npm run trigger && npm run verify
```

Expected: `OK: eslint_sonarjs.json verified — Path Coverage % 100/100`

Primary file: **eslint_sonarjs.json** at repository root.

Branch: **master** only.
