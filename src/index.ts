/** Platform entry — ESLint + eslint-plugin-sonarjs Path Coverage training repo. */
export function bootstrap(): void {
  console.log("[eslint-sonarjs-training] Path Coverage % platform ready");
}

bootstrap();
